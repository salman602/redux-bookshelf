import React from 'react';
import Sidebar from '../components/PageLayout/Sidebar';

const ReadingLIst = () => {
    return (
        <div>
            <Sidebar />
            <h2>Books those are added to the reading list</h2>
        </div>
    );
};

export default ReadingLIst;